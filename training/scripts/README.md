# Scripts

Dataset conversion scripts can be added here in later milestones.

Current shared preprocessing logic lives in `src/ml/landmarks/` and `src/ml/dataset/` so the browser runtime and training pipeline use the same feature representation.

No model training script exists yet.