# Feature Files

Future ML-ready feature files can be placed here.

For static one-hand signs, each hand feature vector contains 63 numerical values. Metadata such as label, sample ID, dataset source, signer ID, and split should remain separate from the vector.